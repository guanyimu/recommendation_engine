#pragma once

#include <cstdint>
#include <string>
#include <vector>

namespace recommendation {

using UserId = std::uint32_t;
using ItemId = std::uint32_t;
using Video = std::string;

}  // namespace recommendation
