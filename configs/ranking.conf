# ranking 服务配置（demo 自动生成）
ranking_address=127.0.0.1:50052
recommend_count=8
# mainserver 调用 ranking 时 WaitForConnected 最长等待秒数
ranking_connect_timeout_sec=5
# Reload/Init 时 WaitDrained 最长等待毫秒数（Ranking RPC 可能 sleep 1～2s，建议 >=3000）
gate_drain_timeout_ms=3000
